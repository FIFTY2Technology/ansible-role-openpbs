---

- name: Clean up temporary downloads
  ansible.builtin.file:
    path: "{{ _download_archive.dest }}"
    state: absent

- name: Restart PBS service
  ansible.builtin.systemd:
    name: pbs
    state: restarted
  when:
    - openpbs_allow_running
